/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(nums, target) {
    for (var i=0;i<nums.length;i++){
        var other = target-nums[i];

        if(nums.indexOf(other)>0){
            console.log(other+':'+nums.indexOf(other));
            return [i,nums.indexOf(other)];
        }
    }
};
